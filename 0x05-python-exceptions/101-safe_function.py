#!/usr/bin/python3

def safe_function(fct, *args):
    import sys
    try:
        var = fce(*args)
        return var
    except Exception as e:
        print("Exception: {}".format(e), file-str.stderr
                return None
